import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // Настройки темы Material-UI
  palette: {
    primary: {
      main: "#1976D2", // Цвет основных элементов
    },
    secondary: {
      main: "#EF6C00", // Цвет второстепенных элементов
    },
  },
});

export default theme;
